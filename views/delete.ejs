<%- include('partials/head', { title: 'Deleted', description: 'Record deleted successfully' }) %>

<body>
    <%- include('partials/navbar', { isAuthenticated: true }) %>
    
    <div class="form-container text-center">
        <div class="card">
            <div class="card-header">
                <h1 class="card-title text-success">
                    <i class="fas fa-check-circle"></i> Record Deleted
                </h1>
            </div>
            <div class="card-body">
                <p style="font-size: 1.1rem; margin: 20px 0; color: var(--text-secondary);">
                    The transaction has been successfully deleted.
                </p>
                <%- include('components/loading') %>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <%- include('partials/scripts') %>
    
    <script>
        setTimeout(() => {
            window.location = "/dash-bord";
        }, 1000);
    </script>
</body>
</html>
